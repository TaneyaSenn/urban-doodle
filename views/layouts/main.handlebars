<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <title>
            {{#if title}}
            {{title}}
            {{else}}
            website.com
            {{/if}}
        </title>
        
        {{!-- bootstrap, CSS & client JS--}}
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
        <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/js/bootstrap.bundle.min.js"></script>
        <link href="app.css" rel="stylesheet" type="text/css">
        <script src="client.js"></script>

    </head>

    <body>
        <nav class="navbar navbar-expand-sm bg-light">

                <ul class="navbar-nav">

                <li class="nav-item">
                    <a href="/" class="nav-link {{#if home}} active {{/if}}">home</a>
                </li>
                <li class="nav-item">
                    <a href="/about" class="nav-link {{#if about}} active {{/if}}">about</a>
                </li>
                <li class="nav-item">
                    <a href="/connect" class="nav-link {{#if contact}} active {{/if}}">connect</a>
                </li>

                {{#if user}}
                <li class="nav-item">
                    <a href="/logout" class="nav-link {{#if logout}} active {{/if}}">log out</a>
                </li>
                {{else}}
                <li class="nav-item">
                    <a href="/login" class="nav-link {{#if login}} active {{/if}}">log in</a>
                </li>
                <li class="nav-item">
                    <a href="/signup" class="nav-link {{#if signup}} active {{/if}}">sign up</a>
                </li>
                {{/if}}

            </ul>

        </nav>


        {{{body}}}    

    </body>

</html>